#include <tunables/global>
profile climate_balancer flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  network inet stream,
  network inet6 stream,
  /usr/bin/python3 ix,
  /usr/lib/** mr,
  /lib/** mr,
  /app/** r,
  /data/** rwk,
  /proc/sys/kernel/random/uuid r,
}
